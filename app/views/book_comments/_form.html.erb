<div class="mt-4">
  <%= form_with model: [@book, @book_comment] do |f| %>
    <div class="mb-2">
      <%= f.text_area :comment, rows: 3, placeholder: "コメントを書く", class: "form-control" %>
    </div>
    <%= f.submit "送信", class: "btn btn-primary" %>
  <% end %>
</div>